const items=[...document.querySelectorAll('.ai-item')];
function setOpen(item,open){item.classList.toggle('open',open);item.querySelector('.ai-summary').setAttribute('aria-expanded',String(open));}
document.querySelectorAll('.ai-summary').forEach(button=>button.addEventListener('click',()=>setOpen(button.closest('.ai-item'),!button.closest('.ai-item').classList.contains('open'))));
document.querySelectorAll('[data-open="agnes"]').forEach(button=>button.addEventListener('click',()=>{const item=document.querySelector('[data-ai="agnes"]');setOpen(item,true);item.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>item.querySelector('input[type="password"]').focus(),350)}));
let allOpen=false;document.querySelector('#toggle-all').addEventListener('click',event=>{allOpen=!allOpen;items.forEach(item=>setOpen(item,allOpen));event.currentTarget.textContent=allOpen?'全部折叠':'全部展开'});
document.querySelectorAll('[data-target]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.side-nav nav button').forEach(item=>item.classList.remove('active'));button.classList.add('active');document.querySelector('#'+button.dataset.target)?.scrollIntoView({behavior:'smooth',block:'start'})}));
const expertGrid=document.querySelector('#expert-grid');function updateCount(){const enabled=[...expertGrid.querySelectorAll('input:checked')].length;document.querySelector('#enabled-count').textContent=String(enabled);expertGrid.querySelectorAll('.expert').forEach(card=>card.classList.toggle('enabled',card.querySelector('input').checked))}expertGrid.addEventListener('change',updateCount);
document.querySelector('#expert-search').addEventListener('input',event=>{const query=event.target.value.trim().toLowerCase();expertGrid.querySelectorAll('.expert').forEach(card=>card.hidden=!card.dataset.name.toLowerCase().includes(query))});
const engine=document.querySelector('#popup-engine'),expert=document.querySelector('#popup-expert'),warning=document.querySelector('#popup-warning');function updatePopup(){const ai=engine.value!=='google',missing=engine.value==='agnes';expert.hidden=!ai||missing;warning.classList.toggle('show',missing)}engine.addEventListener('change',updatePopup);updatePopup();
