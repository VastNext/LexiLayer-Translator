# 语层翻译（LexiLayer Translator）0.10.8

## 修复范围

- 重试只处理失败段落，不再作废整页 generation、断开其他可见性队列或取消整页请求。
- 重试中的段落避免重复提交，失败后可以再次重试。
- 本地恢复不被后台取消拒绝或悬挂阻断，旧会话结果不能写回恢复后的页面。
- 成功译文缓存继续保留，完整重置重新调度未完成段落。

## 边界

针对“快速滚动、部分失败后立即重试，其余段落永久 loading”的调度路径修复，不宣称已验证 Vercel 所有现场情况。不改扫描规则、内联渲染器、超时配置或52 KiB累计预算，不新增全页定时巡检。

## ✅ 验证

本地验证记录：

- 首轮全量单测614项通过，类型检查和生产构建通过。
- 复审修正重试候选后，控制器58项与构建预算2项定向检查通过，类型检查通过；不以首次全量结果替代修正后的定向验证。
- 本地扩展E2E首轮32项通过、新增用例失败于测试标签页的Popup订阅建立；修正测试时序后，新增重试/缓存用例与401、畸形响应两项回归均通过。功能断言和缓存请求计数没有放宽。
- 覆盖四类段落并存、旧重试悬挂时新会话可重试、离屏失败项重复重试只提交一次、取消失败/悬挂、恢复与缓存复用；独立定向复审无阻断问题。

取消通信失败时底层请求可能继续消耗配额；本地恢复不受其阻塞。未针对用户的真实Vercel会话进行在线复现。
