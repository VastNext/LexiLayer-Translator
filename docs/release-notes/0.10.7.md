# 语层翻译（LexiLayer Translator）0.10.7

## 修复范围

- 内联译文写入前验证原文、挂载目标与包装节点的归属，防止向脱离页面的旧节点写入后误计完成。
- 处理原文文字不变但内联包装被替换的结构失效，以及复制插件标记的内联克隆场景。

## 证据边界

用户报告标题仍显示“翻译中…”而 Popup 已显示完成 39/39。本次验收针对本地挂载失效和统计不一致路径，不声称已确认 gameseoradar.com 现场采用了哪一种 DOM 更新方式。

不增加站点专用规则、不强制排空屏外队列、不修改请求超时参数、权限或数据发送目的地。超时配置仍为独立后续需求。

## ✅ 验证

最终本地验收记录：

- 类型检查通过，生产构建成功。
- 全量单测 610 项通过，含体积预算检查。
- 本地扩展 E2E 的 32 项用例均取得通过证据：全套运行遇到启动耗时和整体时限，未完成及失败项定向补测；一项既有划词用例在扩展设置页导航时超时，使用 90 秒启动/用例时限独立复测通过，未放宽功能断言。
- 新增真实重试验证：请求期间角色变为按钮，错误回退可见，点击重试后译文成功且页面角色保持。
- 用户确认累计预算由 48 KiB 调整为 52 KiB；各入口单独上限保持不变。这是项目预算，不是商店限制。

独立代码终审无功能阻断。此记录不代表已创建 GitHub Release 标签或已提交 Chrome Web Store。
