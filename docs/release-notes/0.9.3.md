# 语层翻译（LexiLayer Translator）0.9.3

## 🐞 Bug 修复

- 🧭 **GitHub README 语言切换链接误翻**：支持排除位于 Markdown 内部的语言导航段落（如居中的 `简体中文 · <b>English</b>` 或指向其他语言 README 的链接栏），避免将其作为正文翻译并破坏布局。

## ✅ 验证

- 单元测试：36 个测试文件、523 个测试通过。
- TypeScript 类型检查通过。
- 生产构建与内容脚本体积预算通过。
