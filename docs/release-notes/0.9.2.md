# 语层翻译（LexiLayer Translator）0.9.2

## 🐞 Bug 修复

- 🧩 **正常标题被显示为“消息格式无效”**：跳过超过单段翻译上限的候选节点，避免 Gmail、GitHub 等复杂页面中某个超长容器导致整个批次失败，连带正常的 `🚀 GlanceMD` 等品牌标题也显示错误。
- 🧹 **异步空节点误请求**：初始扫描和动态页面观察都不再把空的 `h1`、`h2`、`p` 等节点送入翻译，减少 Gmail 异步加载期间的无效请求和误报。

## ✅ 验证

- 单元测试：36 个测试文件、522 个测试通过。
- TypeScript 类型检查通过。
- 生产构建与内容脚本体积预算通过。
