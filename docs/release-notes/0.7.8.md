# 语层翻译（LexiLayer Translator）0.7.8

## 🐞 Bug 修复

- 🐙 **GitHub 语言切换误翻**：排除 Repository files 区域中的 `English · 简体中文` 语言导航段落，避免它被当作 README 正文翻译。
- 🧭 **GitHub 导航边界加强**：继续使用稳定的父子结构和语义属性识别仓库导航，不依赖动态生成的 CSS Modules class。

## ✅ 验证

- 单元测试：460 个测试通过。
- TypeScript 类型检查通过。
- 生产构建与内容脚本体积预算通过。
