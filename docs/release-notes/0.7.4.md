# 语层翻译（LexiLayer Translator）0.7.4

## 🐞 Bug 修复

- 🧱 **“仅译文”模式显示源文**：修复部分网站使用高优先级 CSS 覆盖 HTML `hidden` 行为时，源文仍然可见的问题。现在仅译文模式会可靠隐藏源文，同时保留译文显示。

## ✅ 验证

- 单元测试：443 个测试通过。
- TypeScript 类型检查通过。
- 生产构建与内容脚本体积预算通过。
